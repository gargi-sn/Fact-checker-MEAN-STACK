import { TopnavDirective } from './topnav.directive';

describe('TopnavDirective', () => {
  it('should create an instance', () => {
    const directive = new TopnavDirective();
    expect(directive).toBeTruthy();
  });
});
